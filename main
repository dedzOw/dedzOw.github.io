/*created with the help of chatGPT*/

/* main.css */
*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: radial-gradient(circle at top left, #1f283a, #050814 55%, #020308 100%);
  color: #f5f7ff;
}

/* Layout */

.sidebar {
  position: fixed;
  inset: 0 auto 0 0;
  width: 220px;
  padding: 1.5rem 1.75rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: linear-gradient(180deg, rgba(10, 15, 30, 0.95), rgba(4, 6, 14, 0.98));
  border-right: 1px solid rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(18px);
  z-index: 10;
}

.logo {
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  font-size: 0.9rem;
  color: #ffffff;
}

.nav {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.nav a {
  text-decoration: none;
  font-size: 0.9rem;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: rgba(245, 247, 255, 0.7);
  padding: 0.35rem 0.75rem;
  border-radius: 999px;
  transition: background 0.2s ease, color 0.2s ease, transform 0.15s ease;
}

.nav a:hover {
  background: rgba(98, 145, 255, 0.16);
  color: #ffffff;
  transform: translateX(2px);
}

.page {
  margin-left: 220px;
}

/* Sections */

.section {
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding: 4rem 6vw;
}

.section-alt {
  background: radial-gradient(circle at top right, rgba(111, 76, 255, 0.26), transparent 60%);
}

.section-inner {
  max-width: 1080px;
  margin: 0 auto;
  width: 100%;
  display: grid;
  grid-template-columns: minmax(0, 1.1fr) minmax(0, 1fr);
  gap: 3rem;
  align-items: center;
}

.section-intro .section-inner {
  grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.8fr);
}

.text h1,
.text h2 {
  margin: 0 0 0.75rem;
}

.text h1 {
  font-size: clamp(2.4rem, 4vw, 3.4rem);
}

.text h2 {
  font-size: clamp(1.8rem, 3vw, 2.3rem);
}

.text p {
  margin: 0 0 1rem;
  color: rgba(245, 247, 255, 0.75);
  line-height: 1.7;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  margin-top: 0.75rem;
  padding: 0.7rem 1.6rem;
  border-radius: 999px;
  border: 1px solid rgba(255, 255, 255, 0.18);
  background: linear-gradient(135deg, #6f4cff, #3a8dff);
  color: #fff;
  font-size: 0.95rem;
  text-decoration: none;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  cursor: pointer;
  transition: transform 0.1s ease, box-shadow 0.1s ease, filter 0.1s ease;
}

.btn:hover {
  filter: brightness(1.06);
  box-shadow: 0 12px 30px rgba(30, 71, 255, 0.35);
  transform: translateY(-1px);
}

/* Visual elements */

.visual {
  position: relative;
}

/* Orbit graphic */

.orbit {
  width: 260px;
  height: 260px;
  margin-inline: auto;
  border-radius: 50%;
  display: grid;
  place-items: center;
}

.orbit-ring {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 1px solid rgba(111, 148, 255, 0.4);
  box-shadow: 0 0 40px rgba(90, 132, 255, 0.35);
  position: relative;
}

.orbit-dot {
  position: absolute;
  width: 18px;
  height: 18px;
  border-radius: 999px;
  background: radial-gradient(circle, #ffffff 0, #6f4cff 60%, #12152a 100%);
  box-shadow: 0 0 18px rgba(111, 76, 255, 0.8);
  animation: orbit 12s linear infinite;
}

@keyframes orbit {
  0% {
    transform: rotate(0deg) translateX(115px) rotate(0deg);
  }
  100% {
    transform: rotate(360deg) translateX(115px) rotate(-360deg);
  }
}

/* Cards */

.card-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 1rem;
}

.card {
  background: radial-gradient(circle at top left, rgba(255, 255, 255, 0.12), rgba(8, 10, 26, 0.98));
  border-radius: 1.25rem;
  padding: 1.2rem 1.3rem;
  border: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 0 14px 40px rgba(0, 0, 0, 0.38);
}

.card h3 {
  margin: 0 0 0.5rem;
  font-size: 1.05rem;
}

.card p {
  margin: 0;
  font-size: 0.9rem;
  color: rgba(245, 247, 255, 0.7);
}

/* Spotlight layout */

.spotlight {
  grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
}

.spotlight.reverse {
  grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
}

.spotlight.reverse .text {
  order: 2;
}

.spotlight.reverse .visual {
  order: 1;
}

.project-cards {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 1rem;
}

/* Lists */

.feature-list {
  list-style: none;
  padding: 0;
  margin: 1rem 0 0;
  display: grid;
  gap: 0.35rem;
}

.feature-list li {
  position: relative;
  padding-left: 1.4rem;
  font-size: 0.95rem;
}

.feature-list li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0.55rem;
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: linear-gradient(135deg, #6f4cff, #3a8dff);
}

/* Contact section */

.section-contact {
  grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
}

.contact-form {
  display: grid;
  gap: 0.9rem;
  margin-top: 0.5rem;
}

.field {
  display: grid;
  gap: 0.25rem;
}

.field label {
  font-size: 0.85rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(245, 247, 255, 0.7);
}

input,
textarea {
  padding: 0.6rem 0.8rem;
  border-radius: 0.6rem;
  border: 1px solid rgba(255, 255, 255, 0.15);
  background: rgba(3, 5, 18, 0.9);
  color: #f5f7ff;
  font: inherit;
  resize: vertical;
}

input:focus,
textarea:focus {
  outline: none;
  border-color: rgba(94, 145, 255, 0.9);
  box-shadow: 0 0 0 1px rgba(94, 145, 255, 0.7);
}

.contact-meta {
  display: grid;
  gap: 1rem;
}

.meta-block {
  padding: 0.9rem 1rem;
  border-radius: 0.9rem;
  border: 1px dashed rgba(255, 255, 255, 0.16);
  background: rgba(5, 7, 20, 0.9);
}

.meta-block h3 {
  margin: 0 0 0.4rem;
  font-size: 0.95rem;
}

.meta-block p {
  margin: 0;
  font-size: 0.9rem;
  color: rgba(245, 247, 255, 0.75);
}

.meta-block a {
  color: #7ea3ff;
  text-decoration: none;
}

.meta-block a:hover {
  text-decoration: underline;
}

/* Footer */

.footer {
  padding: 1.5rem 6vw 2rem;
  text-align: center;
  font-size: 0.8rem;
  color: rgba(245, 247, 255, 0.55);
}

/* Responsive */

@media (max-width: 900px) {
  .sidebar {
    position: fixed;
    inset: 0 0 auto 0;
    width: 100%;
    height: auto;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    padding-inline: 5vw;
  }

  .nav {
    flex-direction: row;
    gap: 0.25rem;
  }

  .nav a {
    font-size: 0.75rem;
    padding-inline: 0.7rem;
  }

  .page {
    margin-left: 0;
    padding-top: 70px;
  }

  .section {
    padding-top: 4.5rem;
    padding-bottom: 3.5rem;
  }

  .section-inner,
  .section-intro .section-inner,
  .spotlight,
  .spotlight.reverse,
  .section-contact {
    grid-template-columns: minmax(0, 1fr);
  }

  .spotlight.reverse .text,
  .spotlight.reverse .visual {
    order: initial;
  }

  .card-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 640px) {
  .section {
    padding-inline: 5vw;
  }

  .card-grid {
    grid-template-columns: minmax(0, 1fr);
  }
}
